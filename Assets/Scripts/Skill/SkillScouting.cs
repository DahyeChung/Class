public class SkillScouting : SkillBase
{
    public override void Init()
    {
        base.Init();
        SkillType = Define.SkillType.FireflyScouting;
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
