
/// <summary>
/// All Test Scene UI comes here.
/// </summary>
public class UI_TestScene : UI_Scene
{
    #region Events Bindings

    // Default UI structure for the scene
    enum Buttons
    {

    }

    #endregion

    #region Popup in the Scene

    UI_QuestPopup _questPopupUI;

    #endregion

    /* What does this class do?
     * 1. Define interactable UI elements in the scene. (Enum)
     * 2. Collect all Popup & Toast UIs in the scene.
     * 3. Bind events to the UI elements.
     * 4. Initialize.
     * 5. Show UI & OnClick events
     */


    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
